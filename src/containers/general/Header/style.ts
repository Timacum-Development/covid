import styled from 'styled-components';

export const HeaderWrap = styled.header`
	display: flex;
	justify-content: space-between;
	align-items: center;
	width: 100%;
	padding: 35px 0 50px 0;
	position: absolute;
	left: 50%;
	transform: translateX(-50%);
	z-index: 1;
`;

export const NavCtaWrap = styled.div`
	display: flex;
	align-items: center;
`;
